/** Desktop only: orb WebGL (pointer fin + largeur md+) */
export const DESKTOP_ORB_MEDIA = "(min-width: 768px) and (pointer: fine)"

/** Mobile / tactile : pas d’orb (dégradé CSS uniquement) */
export const MOBILE_ORB_MEDIA = "(max-width: 767px), (pointer: coarse)"
